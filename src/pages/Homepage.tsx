import * as React from "react"
import {
    Flex,
    ChakraProvider,
    Box,
    Text,
    Link,
    VStack,
    Code,
    Grid,
    theme,
} from "@chakra-ui/react"

export const Homepage = () => (
    <React.Fragment>
        <Flex
            direction='column'
            align='center'
        >
        </Flex>
    </React.Fragment>
)
